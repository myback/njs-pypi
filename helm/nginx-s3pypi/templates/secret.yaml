{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nginx-s3pypi.fullname" . }}
  labels:
{{ include "nginx-s3pypi.labels" . | indent 4 }}
type: Opaque
data:
{{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc }}
{{- end }}
{{- end }}
